# Public open-core workspace. Keep the node-modules layout strict (each package
# only sees its declared deps — the discipline that proves the open-core
# boundary holds).
auto-install-peers=true
strict-peer-dependencies=false
